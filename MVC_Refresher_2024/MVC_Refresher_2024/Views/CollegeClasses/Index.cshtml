@model IEnumerable<MVC_Refresher_2024.Models.CollegeClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.RequestId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CollegeClassName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RequestId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CollegeClassName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CollegeClassID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CollegeClassID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CollegeClassID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
